- utilité de log4php dans le code actuel ? garder pour après ?
- sécuriser : content avec les infos saisies, vérif et transformation côté script
- script : gérer cas d'erreur
- possibilité config redirection automatique vers HTTPS only
- rabbitmq
    - installer
    - code d'exemple
- vhffs db
    - connexion postgresql VHFFS
    - fonction hash mdp vhffs ? (login admindl direct)
    - liste user project domains avec webroot (calculé ?)
- interface visu
    - saisie juste un domaine, fais le reste
    - recherche par user project domain avec auto complétion , filtrage live (penser gros volume)
    - affichage stats HTTPS, demandes en cours
    - possibilité d'erreur, afficher logs, annulation, relance ultérieure
- renouvellement automatique
    - mémoriser l'etat et la date des certificats
    - cron chaque jour, renouveler ceux à qui il reste moins d'un mois (utiliser la file de messages aussi)